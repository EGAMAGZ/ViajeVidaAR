import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.patrones.viajevidaar',
  appName: 'ViajeVidaAR',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
